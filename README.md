# Sentrix

Behaviour-based anomaly detection for cloud activity logs, producing
risk-scored security alerts through an API and dashboard.

**Status:** in development.

## Tech stack
Python, scikit-learn, FastAPI, PostgreSQL, Docker, AWS

## Setup
1. `python -m venv venv` and activate it
2. `pip install -r requirements.txt`
3. `cp .env.example .env` and fill in values

## Structure
See `docs/` (to be added).